require "spec_helper"

describe AsyncEventEmitter do
  it "has a version number" do
    expect(AsyncEventEmitter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
